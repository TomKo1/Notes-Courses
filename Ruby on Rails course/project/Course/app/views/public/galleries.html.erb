<% @title_page = @gallery.name %>

<div class="container">
    <h2><%= @gallery.name %></h2>
    <div class="row">
        <div class="col-md-8">
            <%= @gallery.description %>
            <hr>
            <p><%= link_to('Return',{:controller=>'public', :action => 'index'},:class => 'btn btn-default')%></p>
        </div>
        <div class="col-md-4">
            <%= image_tag(@gallery.picture.url) %>
        </div>
    </div>
</div>
