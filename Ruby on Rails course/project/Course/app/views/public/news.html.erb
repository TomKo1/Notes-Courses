<% @title_page = @article.name %>
<div class="container">
    <h2><%= @article.name %></h2>

    <div id="row">
        <div class="col-md-8">
            <p><%= @article.content %></p>
            <hr>
            <p><%= link_to('Return', {:controller => 'public', :action => 'index'}, :class => 'btn btn-default')%></p>
        </div>
        <div class="col-md-4">
            <%= image_tag(@article.pictures.url, :class => 'featurette-image img-responsive') %>
        </div>
    </div>
</div>
